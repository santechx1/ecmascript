/* Includes*/
// Método de array para buscar un determinado elemento / Array method to search for an specific element
// es7
let numbers = [1, 2, 3, 4, 5, 6, 7, 8];
if (numbers.includes(9)) {
    console.log('Found!');
} else {
    console.log('Not found!');
}

/*Exponential / Elevar a una potencia */
// es7
let base = 4;
let exponent = 4;
let result = base ** exponent;
console.log(result);
